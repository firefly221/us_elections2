# US Elections Flask API - v0.1

Pierwsza wersja ma tylko szkielet aplikacji Flask i healthcheck.

## Endpointy

- `GET /api/health`

## Jenkins

```powershell
pip install -r requirements.txt
python -m unittest discover -s tests -v
```
